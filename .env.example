# Supabase public client configuration (the anon key is safe to expose in the browser when RLS is correctly configured).
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"

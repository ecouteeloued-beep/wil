# خلية الإصغاء والتكفل بانشغالات المواطن — ولاية الوادي

بوابة إلكترونية عربية لتقديم وتتبع انشغالات المواطنين، مع لوحة إدارة متعددة الأدوار ومصادقة Supabase Auth.

## التشغيل

```bash
npm install
npm run dev
npm run lint
npm run build
```

## إعداد Supabase

1. أنشئ مشروع Supabase واضبط `VITE_SUPABASE_URL` و`VITE_SUPABASE_ANON_KEY`.
2. نفّذ `supabase/schema.sql` من SQL Editor.
3. أنشئ مستخدمي الإدارة من **Authentication → Users**.
4. استخدم `supabase/admin_accounts.sql` لربط حسابات Auth بجدول `public.users`.
5. استخدم `supabase/role_permissions.sql` لتطبيق صلاحيات الأدوار الحكومية الدقيقة.
6. استخدم `supabase/admin_management.sql` لتعديل الحسابات وإسناد الشكاوى وإضافة الردود.

## نموذج الصلاحيات حسب بروتوكول العمل

- **الوالي (`wali`):** صاحب السلطة الإدارية الشاملة واعتماد القرارات والردود.
- **الأمين العام (`chef_cabinet`):** إدارة ومتابعة شاملة وتنسيق الملفات والردود ضمن التفويض الإداري.
- **رئيس الديوان (`head_department`):** إدارة ملفات الديوان أو المصلحة التابعة له.
- **رئيس الخلية (`supervisor`):** فرز الشكاوى وتوجيهها ومراجعتها ومتابعة الموظفين.
- **الموظف المكلف (`employee`):** معالجة الملفات المسندة إليه وإعداد الردود دون اعتماد نهائي.
- **المشرف التقني (`super_admin`):** إدارة النظام والأمن والمستخدمين تقنيًا، دون التحكم في الشكاوى أو القرارات الإدارية.

تُفرض الصلاحيات داخل Supabase RLS، وليس فقط عبر إخفاء أزرار الواجهة.

## الأمان

- لا توجد كلمات مرور أو PIN ثابتة داخل المصدر.
- دخول الإدارة يتم عبر Supabase Auth.
- تتبع المواطن يتم عبر رقم التتبع ورقم الهاتف من خلال دالة مقيدة.
- القراءة والتعديل الإداريان محميان بسياسات RLS حسب الدور ونطاق الإسناد.
- البيانات الداخلية وسجل التدقيق غير متاحين للزوار.

لا تضع `service_role key` داخل الواجهة أو أي متغير بيئة يبدأ بـ `VITE_`.

## الإصدار الإنتاجي

يعتمد الإصدار الإنتاجي على Supabase Auth وRow Level Security، ويستخدم وظائف قاعدة بيانات مقيّدة الصلاحيات لإيداع وتتبع العرائض. تم تجهيز فهارس سير العمل الأساسية لدعم نمو عدد العرائض والرسائل وسجلات التدقيق. يجب ضبط `VITE_SUPABASE_URL` و`VITE_SUPABASE_ANON_KEY` في Vercel، وإنشاء حسابات الموظفين من Supabase Auth، والتحقق من النسخ الاحتياطي قبل فتح الاستقبال العام.

قبل الاعتماد الرسمي، يجب اختبار الصلاحيات لكل دور، تفعيل حماية كلمات المرور المسربة من إعدادات Supabase Auth، وتنفيذ تجربة استرجاع لنسخة احتياطية.
